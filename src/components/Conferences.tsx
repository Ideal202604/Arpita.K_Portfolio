import React from 'react';

// Conferences section removed per request (ics-community-app removed).
// Keep component but render null so imports and layout stay stable.
const Conferences: React.FC = () => {
  return null;
};

export default Conferences;
